  <tr>
   <%loop_length= [@deal_attributes_first.count,@deal_attributes_second.count].max%>
    <%(0...loop_length).each_with_index do |attribute,index|%>
    <table class="table table-bordered">
      <tr>
        <th scope="row" >Internet</th>
        <th></th>
        <th></th>
      </tr>
      <tr>
        <td width="30%">Download (mbps)</td>
        <td width="30%" class="text-center"><%= @deal_attributes_first[index].try(:download).present? ? "#{@deal_attributes_first[index].try(:download)}" : "N/A" %></td>
        <td width="30%" class="text-center"><%= @deal_attributes_second[index].try(:download).present? ? "#{@deal_attributes_second[index].try(:download)}" : "N/A"%></td>
      </tr>
      <tr>
        <td>Upload (mbps)</td>
        <td class="text-center"><%= @deal_attributes_first[index].try(:upload).present? ? "#{@deal_attributes_first[index].try(:upload)}" : "N/A"%></td>
        <td class="text-center"><%=@deal_attributes_second[index].try(:uploadd).present? ? "#{@deal_attributes_second[index].try(:uploadd)}" : "N/A"%></td>
      </tr>
    </table>
    <% end %>
  </tr>

  <%loop_length= [@deal_equipment_first.count,@deal_equipment_second.count].max%>
<%if loop_length > 0 %>
<tr>
   <table>

      <tr class="row-differ">
    <th scope="row" >Equipment &amp; Installation</th>
    <th></th>
    <th></th>
  </tr>
   </table>
</tr>
  <tr>
   <%loop_length= [@deal_equipment_first.count,@deal_equipment_second.count].max%>
    <%(0...loop_length).each_with_index do |attribute,index|%>
    <table class="table table-bordered">
      <tr>
        <th width="30%">Name</th>
        <th width="30%" class="text-center"><%=@deal_equipment_first[index].try(:name).present? ? @deal_equipment_first[index].try(:name) : "N/A"%></th>
        <th width="30%" class="text-center"><%= @deal_equipment_second[index].try(:name).present? ? @deal_equipment_second[index].try(:name) : "N/A"%></th>
      </tr>
       <tr>
        <td>Price /mon</td>
        <td class="text-center"><%= @deal_equipment_first[index].try(:price).present? ? "$#{"%.2f" % @deal_equipment_first[index].try(:price)}" : "$0.00"%></td>
        <td class="text-center"><%= @deal_equipment_second[index].try(:price).present? ? "$#{"%.2f" % @deal_equipment_second[index].try(:price)}" : "$0.00"%></td>
      </tr>
      <tr>
        <td>Installation Charges</td>
        <td class="text-center"><%=  @deal_equipment_first[index].try(:installation).present? ? @deal_equipment_first[index].try(:installation) : "N/A"%></td>
        <td class="text-center"><%=@deal_equipment_second[index].try(:installation).present? ? @deal_equipment_second[index].try(:installation) : "N/A" %></td>
      </tr>
    </table>
    <% end %>
  </tr>
<% end %>